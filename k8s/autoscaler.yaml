apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
  name: "$service_name"
spec:
  maxReplicas: 20
  minReplicas: 1
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: "$service_name"
  targetCPUUtilizationPercentage: 60
status:
  currentReplicas: 0
  desiredReplicas: 0
